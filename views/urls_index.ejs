<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Tiny App</title>
</head>
<body>
  <% include partials/_header %>
  <ul>
    <%= JSON.stringify(urls) %>
    <% for (var key in urls) { %>
      <% const deleteAction = `/urls/${key}/delete` %>
      <li><%= `${key}: ${urls[key]}` %><form method="POST" action="<%= deleteAction %>"><input type="submit" value="Delete"></form></li>
    <% } %>
  </ul>
</body>
</html>